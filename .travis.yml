services:
  - mysql
before_install:
  - mysql -e "create database IF NOT EXISTS moodb;" -uroot
language: node_js
node_js:
  - "node"
before_script:
  - webpack
  - npm test
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
after_script:
- echo "Build Finished!"
